name: EduFlow CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven

      - name: Build & Test
        run: mvn clean verify

  build-and-push-docker:
    needs: build-and-test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service:
          - eureka-server
          - api-gateway
          - identity-service
          - course-service
          - enrollment-service
          - notification-service

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.service }}
          tags: |
            type=sha
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # deploy-to-k8s:
  #   needs: build-and-push-docker
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Kustomize
  #       uses: imranismail/setup-kustomize@v2

  #     - name: Update Kubernetes images
  #       run: |
  #         cd k8s
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/eureka-server:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/eureka-server:sha-${{ github.sha }}
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/api-gateway:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/api-gateway:sha-${{ github.sha }}
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/identity-service:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/identity-service:sha-${{ github.sha }}
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/course-service:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/course-service:sha-${{ github.sha }}
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/enrollment-service:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/enrollment-service:sha-${{ github.sha }}
  #         kustomize edit set image ghcr.io/\${GITHUB_REPOSITORY}/notification-service:latest=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/notification-service:sha-${{ github.sha }}

  #     - name: Deploy to Kubernetes
  #       uses: azure/k8s-deploy@v4
  #       with:
  #         action: deploy
  #         manifests: |
  #           k8s/
  #         strategy: basic
  #         kustomize: true
  #         kubeconfig: ${{ secrets.KUBE_CONFIG }}
